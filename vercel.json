{
    "version": 2,
    "builds": [
        {
            "src": "src/*.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "(.*)",
            "dest": "/src"
        }
    ],
    "env": {
        "MONGODB_URI": "LINK_MONGODB"
    }
}
